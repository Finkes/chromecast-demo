{
  "name": "chromecast-demo",
  "version": "1.0.0",
  "description": "This Google Cast demo application shows how to send messages from an Chrome desktop browser to a receiver using a custom namespace.",
  "main": "index.js",
  "scripts": {
    "start" : "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Finkes/chromecast-demo.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.9.0"
  }
}
